const {describe, it} = require("node:test");
const {strictEqual, deepStricEqual} = require("assert");

describe("verify-guess", () => {
  it("should be correct if the guess is correct", () => {
    const socket = {
      write: 
    }
  })
})